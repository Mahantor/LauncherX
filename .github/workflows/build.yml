name: Build LauncherX for Windows 11 x64

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup .NET 8.0
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
          dotnet-quality: 'ga'

      - name: Restore NuGet packages
        run: dotnet restore LauncherX.sln

      - name: Build LauncherX (x64 Release)
        run: dotnet build LauncherX.sln -c Release -p:Platform=x64 --no-restore

      - name: Publish LauncherX (self-contained x64)
        run: dotnet publish LauncherXWinUI/LauncherXWinUI.csproj -c Release -p:Platform=x64 -p:RuntimeIdentifier=win-x64 -p:SelfContained=true -p:WindowsPackageType=None --output publish/x64 --no-restore -p:PublishReadyToRun=false -p:PublishTrimmed=false

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: LauncherX-Windows11-x64
          path: publish/x64/
          compression-level: 6
          if-no-files-found: error
